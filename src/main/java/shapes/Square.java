package shapes;

import java.awt.*;

public class Square extends Rect {
    public Square(Point initPos, Color col, int side){ // The constructor
        super(initPos, col, side, side); // The constructor
    }
}
